#!/usr/bin/make -f
# Debian rules file for wizard-cli

%:
	dh $@

override_dh_auto_build:
	$(MAKE) CFLAGS="-std=c11 -Wall -Wextra -O2 -D_GNU_SOURCE $$(pkg-config --cflags alsa)"

override_dh_auto_install:
	$(MAKE) install DESTDIR=$(CURDIR)/debian/wizard-cli PREFIX=/usr
